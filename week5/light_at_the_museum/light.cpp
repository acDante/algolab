#include <iostream>
#include <vector>
#include <algorithm>

void testcase(){
    int N, M;  //N: # of switches, M: # of rooms
    std::cin >> N >> M;

}

int main(){
    std::ios_base::sync_with_stdio(false);
    int t; std::cin >> t;
    for(int i = 0; i < t; ++i){
        testcase();
    }
    return 0;
}